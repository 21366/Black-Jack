#pragma once
#include <algorithm>//Organiza elementos en un orden no descendente o de acuerdo con un criterio de ordenación especificado por un predicado binario
#include <sstream>
#include <iostream>
#include <fstream>

int convertir(std::string);

bool convertirBool(std::string);
